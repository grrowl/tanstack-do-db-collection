// tanstack-do-db-collection — wire protocol (shared between server and client).
//
// Frame types, the binary codec (M2), and the tagged value codec (M1/D17).
// Pure TypeScript with no runtime-specific imports, so both the workerd server
// and the browser client can depend on it.
//
// M0 establishes the package layout; implementation follows.

export {}
